import React from 'react';
import {
    View,
    StyleSheet,
} from 'react-native';

export default function SampleListMap () {

    return (
        <View style={StyleSheet.body} >

        </View>
    );
};

const styles = StyleSheet.create({
    body: {
        flex: 1,
        alignItems: 'center',
    }
});
